package tina.coffee.system.exceptions.mapper;

public @interface PackageInfo {

    /**
     *
     * tina.coffee.system.exceptions.mapper 是统一管理 Jersey     的 exception
     * tina.coffee.system.exceptions.handler是统一管理 Spring MVC 的 exception
     *
     * 尽管此处造成了代码重复，但是对于框架的随时转变能起到很好的快速切换作用
     *
     */

}
